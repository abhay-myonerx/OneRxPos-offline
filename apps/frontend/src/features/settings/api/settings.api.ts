/**
 * Extension point for miscellaneous settings endpoints.
 *
 * Tenant and receipt configuration use `@/features/tenant/api/tenant.api` and
 * `@/features/receipt/api/receipt.api`. Use this module for additional settings slices.
 */
export {};
