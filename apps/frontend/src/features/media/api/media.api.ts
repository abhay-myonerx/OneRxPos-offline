/**
 * Extension point for media/asset RTK Query endpoints.
 *
 * Product images primarily use uploads (e.g. Cloudinary) via components under
 * `@/features/media`. Add API-backed media operations here when needed.
 */
export {};
